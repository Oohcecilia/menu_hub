services:
  api:
    container_name: menu_hub
    build: .
    restart: always
    env_file:
      - ../.env
    ports:
      - "7777:7777"
    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 3307
      DB_USER: root
      DB_PASSWORD: x1root99
      DB_NAME: x-pp